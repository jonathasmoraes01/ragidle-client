/AI/*
/client/BGM/*
/client/data/*
/client/resources/*
/client/System/*
/dist/
/node_modules/
/src/Plugins/*/
/index.html
Config.local.js
/package-lock.json
/GrannyModelViewer.js
/GrfViewer.js
/MapViewer.js
/ModelViewer.js
/Online.js
/StrViewer.js
/ThreadEventHandler.js
/data/*
/save/*

# Fonte do rAthena de REFERÊNCIA (o oráculo do M0), copiada para cá conforme o
# DOCKER.md do upstream. Não versionamos: é código de terceiro, ~110 MB, e a
# mesma regra que o Rag Idle já aplica às três raízes externas (D-266).
# Os poucos arquivos que o fork versiona aqui (builder.sh, inter_conf.txt,
# web_conf.txt) continuam rastreados — gitignore não afeta arquivo já rastreado.
/rathena/*

# IDE files
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
!.vscode/*.code-snippets
.vercel
.env*

# Worktrees de agente (D-266): pastas locais, nunca versionadas.
# Entraram por engano num `git add -A` em 21/09/2026 e sairam no commit
# seguinte — o `git add` de uma pasta com `.git` dentro cria um gitlink
# que quebra o clone de quem puxar.
.wt-*/
